#!/bin/bash
#put generated file in equivalent directory under out/

outfile=out/$(dirname $1)/$(basename $1 .md).pdf
mkdir -p "$(dirname $outfile)"
pandoc -o "$outfile" "$1" --data-dir=$(dirname $1)
echo "built $outfile"

