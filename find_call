#!/bin/bash
#put generated file in equivalent directory under out/

dir=$(dirname $1)
file=$(basename $1)

outdir=out/$dir
outfile=$(basename $file .md).pdf
echo $dir
echo $file
echo $outdir
echo $outfile
echo "#######"
# mkdir -p "$outdir"
# cd "$dir"
# pwd
# pandoc -o "$outfile".pdf "$outfile".md
# echo "built $outfile"

